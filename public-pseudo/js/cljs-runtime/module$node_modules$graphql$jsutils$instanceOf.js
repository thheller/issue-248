shadow$provide.module$node_modules$graphql$jsutils$instanceOf = function($global$$, $process$$, $require$$, $module$$, $exports$$) {
  Object.defineProperty($exports$$, "__esModule", {value:!0});
  $exports$$.default = $process$$ ? function($exports$$, $require$$) {
    if ($exports$$ instanceof $require$$) {
      return !0;
    }
    if ($exports$$) {
      var $className$$ = $exports$$.constructor;
      $require$$ = $require$$.name;
      if ($className$$ && $className$$.name === $require$$) {
        throw Error("Cannot use " + $require$$ + ' "' + $exports$$ + '" from another module or realm.\n\nEnsure that there is only one instance of "graphql" in the node_modules\ndirectory. If different versions of "graphql" are the dependencies of other\nrelied on modules, use "resolutions" to ensure only one version is installed.\n\nhttps://yarnpkg.com/en/docs/selective-version-resolutions\n\nDuplicate "graphql" modules cannot be used at the same time since different\nversions may have different capabilities and behavior. The data from one\nversion used in the function from another could produce confusing and\nspurious results.');
      }
    }
    return !1;
  } : function($exports$$, $require$$) {
    return $exports$$ instanceof $require$$;
  };
};
